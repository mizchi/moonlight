// Moonlight - SVG Element Data Model
// 型定義は @model パッケージに移動済み

// 型エイリアス（後方互換性のため）
pub type Point = @model.Point
pub type Style = @model.Style
pub type ShapeType = @model.ShapeType
pub type Element = @model.Element
pub type BoundingBox = @model.BoundingBox
pub type HandlePosition = @model.HandlePosition
