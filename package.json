{
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:component": "moon build --target js && rolldown -c rolldown.config.mjs",
    "build:all": "vite build && pnpm build:component",
    "deploy": "pnpm build:all && wrangler deploy",
    "worker:dev": "pnpm build:all && wrangler dev",
    "test": "playwright test",
    "test:e2e": "playwright test",
    "test:unit": "node --no-warnings -e \"const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json'));delete p.type;fs.writeFileSync('package.json',JSON.stringify(p,null,2))\" && (moon test -p internal/app/model; moon test -p internal/app/components); node --no-warnings -e \"const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json'));p.type='module';fs.writeFileSync('package.json',JSON.stringify(p,null,2))\""
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.3",
    "happy-dom": "^20.0.11",
    "playwright": "^1.57.0",
    "puppeteer": "^24.34.0",
    "rolldown": "1.0.0-beta.57",
    "tsdown": "^0.18.3",
    "vite": "8.0.0-beta.5",
    "vite-plugin-moonbit": "^0.1.0",
    "wrangler": "^4.54.0"
  },
  "type": "module"
}